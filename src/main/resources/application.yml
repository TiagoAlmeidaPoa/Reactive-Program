spring:
  data:
    mongodb:
      uri: mongodb+srv://${DB_USER:admin}:${DB_PASSWORD}@webfluxclustercourse.bqmyxft.mongodb.net/${DB_NAME:webflux-course-db}?retryWrites=true&w=majority
      auto-index-creating: true
  application:
    name: webflux-course